# base - RADV VANGOGH (Steam Deck)
# WINEFSYNC jest ustawiany przez GE automatycznie

# shader compilation - kluczowe dla VANGOGH
RADV_PERFTEST=gpl
DXVK_ASYNC=1
DXVK_STATE_CACHE_PATH="$HOME/.cache/dxvk-state"

# threaded GL (DX9/DX11 games)
mesa_glthread=true

# ciche logi - perf
PROTON_LOG=0
